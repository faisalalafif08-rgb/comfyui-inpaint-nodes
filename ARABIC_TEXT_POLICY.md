# ARABIC_TEXT_POLICY.md

## قاعدة ذهبية
الأصل (raw) لا يُلمس. ننتج نسخة normalized للبحث.

## مشاكل الترميز وحلها
- جرّب: UTF-8 → UTF-8-SIG → CP1256 → fallback(ignore)
- خزّن كل نواتج النص UTF-8.

## RTL والعرض
- التخزين: نص طبيعي في best.txt
- العرض: RTL/CSS/BiDi في الواجهة فقط، بدون تغيير النص المخزن.

## التطبيع (Normalization)
- best.txt: أقرب للأصل
- best.norm.txt: نسخة للبحث:
  - إزالة التشكيل
  - توحيد (أ/إ/آ → ا)
  - ى→ي، ة→ه (اختياري)
  - تنظيف علامات غير ضرورية

## اللهجات
- طبقة dialect_normalizer اختيارية حسب الشخصية
- Lexicon للهجات (لبناني/حجازي) يُستخدم للنطق والبحث.
